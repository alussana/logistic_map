#include <iostream>
#include "bifurcationplot.hpp"
#include "population.hpp"

int main() {
  // /*
  BifurcationPlot bifplot = BifurcationPlot(0, 4, 0.05, 0.25, 1000000);
  bifplot.scan_r("test.txt");
  // */
  /*
  Population test_pop = Population(3.5, 0.25);
  std::vector<double> v = test_pop.find_steady_states();
  for (int i = 0; i < v.size(); i++)
  {
    std::cout << v[i] << "\n";
  }
  */
  return 0;
}